# Copia este archivo como ".env" y rellena tus datos reales de MySQL.
# El archivo ".env" NUNCA se sube a GitHub (ya está en .gitignore).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=pawcare

PORT=8080
